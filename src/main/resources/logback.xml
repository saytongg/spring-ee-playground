<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Define the log file location and pattern -->
    <!-- <property name="LOGS" value="./logs" /> -->

    <!-- Console Appender -->
    <!-- <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender> -->
    
    <appender name="json" class="ch.qos.logback.core.ConsoleAppender">
	    <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
	        <jsonFormatter
	            class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
	            <prettyPrint>true</prettyPrint>
	        </jsonFormatter>
	        <timestampFormat>yyyy-MM-dd' 'HH:mm:ss.SSS</timestampFormat>
	    </layout>
	</appender>

    <!-- RollingFile Appender: for writing logs to a file -->
    <!-- <appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory> Retain logs for 30 days
            <totalSizeCap>10GB</totalSizeCap> Maximum size of logs before purging
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender> -->
    
    <!-- <logger name="jsonLogger" level="TRACE">
	    <appender-ref ref="json" />
	</logger> -->

    <!-- Root Logger -->
    <root level="TRACE">
        <appender-ref ref="json"/>
        <!-- <appender-ref ref="ROLLING"/> -->
    </root>

    <!-- Loggers for specific packages -->
    <!-- <logger name="com.example" level="DEBUG"/>
    <logger name="org.springframework" level="WARN"/> -->

</configuration>
